import 'package:flutter/material.dart';


class ProfilePageFinal extends StatelessWidget {
  const ProfilePageFinal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xff121421),
      body: Container(),
    );
  }
}
